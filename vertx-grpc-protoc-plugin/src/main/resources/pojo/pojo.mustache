{{#packageName}}
package {{packageName}};
{{/packageName}}

import lombok.Data;
import lombok.experimental.Accessors;
{{#imports}}
import {{.}};
{{/imports}}

@Data
@Accessors(chain = true)
public class {{className}} {
{{#fields}}
    private {{javaType}} {{name}};
{{/fields}}

    static public {{protoFullName}} toProto({{className}} pojo) {
        {{protoFullName}}.Builder builder = {{protoFullName}}.newBuilder();
{{#fields}}
{{#isNullable}}
        if (pojo.{{name}} != null) {
{{#isMessage}}
            builder.{{setter}}({{javaType}}.toProto(pojo.{{name}}));
{{/isMessage}}
{{^isMessage}}
            builder.{{setter}}({{nullableType}}.of(pojo.{{name}}));
{{/isMessage}}
        }
{{/isNullable}}
{{^isNullable}}
        builder.{{setter}}(pojo.{{name}});
{{/isNullable}}
{{/fields}}
        return builder.build();
    }

    static public {{className}} fromProto({{protoFullName}} proto) {
        {{className}} pojo = new {{className}}();
{{#fields}}
{{#isNullable}}
        if (proto.{{hasFunction}}()) {
{{#isMessage}}
            pojo.{{setter}}({{javaType}}.fromProto(proto.{{getter}}()));
{{/isMessage}}
{{^isMessage}}
            pojo.{{setter}}(proto.{{getter}}().getValue());
{{/isMessage}}
        }
{{/isNullable}}
{{^isNullable}}
        pojo.{{setter}}(proto.{{getter}}());
{{/isNullable}}
{{/fields}}
        return pojo;
    }
}